description "FanControl startup service"

start on started system-services
stop on runlevel [016]

respawn

exec INSTALL_DIR=$(cat /usr/local/bin/ChromeOS_PowerControl.install_dir) && $INSTALL_DIR/powercontrol __monitor__ >> /var/log/fancontrol.log 2>&1
